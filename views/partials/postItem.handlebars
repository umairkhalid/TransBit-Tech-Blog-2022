<div class="card">
  <div class="card-header  flex-row justify-space-between">
    <h2>
      <a href="/post/{{id}}">
        <div class="col-8">{{title}}</div>
      </a>
    </h2>
    <div class="col-3">posted by {{user.name}} on {{format_date date_created}}
    </div>
  </div>  
  <p class="card-body">
   {{content}}
  </p>
</div>