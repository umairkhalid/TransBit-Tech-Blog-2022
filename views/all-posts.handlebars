<div class="row">
  <div class="col-auto">
    <h2 class="truncate font-bold mb-2 md:mt-4 text-4xl text-gray-800 tracking-normal">Welcome, {{name}}!</h2>
  </div>
</div>
<br>
{{#if posts.length}}
<div class="card flex-column">
  {{#each posts as |post|}}
  {{> editItem post}}
  {{/each}}
</div>
{{/if}}